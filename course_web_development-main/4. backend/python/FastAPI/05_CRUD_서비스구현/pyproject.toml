[project]
name = "sqlite-blog-api"
version = "0.1.0"
description = "FastAPI SQLite blog CRUD lesson"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.115.12",
    "uvicorn[standard]==0.34.3",
]

